# artifacts
__pycache__
.pytest*
*.egg-info
*/build/*
*/dist/*

# misc
.coverage
.vscode
coverage.xml
.ruff_cache
.mypy_cache
.claude/settings.local.json

# hevy-brain local state (personal data + machine-specific config stay local)
config.toml
data/
vault_staging/
logs/
exports/
.mcp.json
coordination/   # session write-claims / fleet runtime live in the vault dev zone, not the repo

# Secrets — never commit real env files
.env
.env.*
!.env.example
