---
status: done
agent: slice1-builder-1
goal: "Slice 1: routines sync + edit (F1+F2) + D1 cursor fix"
outcome: "All three asks shipped; 85 offline tests green (was 60), ruff clean, pushed (aebd6db)"
gotchas: "PUT body verified against the parsed OpenAPI spec only — first live `push routine --dry-run` is the real-API verification; managed routine notes regenerate hourly, so edits belong in a Drafts/ copy"
carry-on: "E3 knowledge bridge + E5 provenance labels (see prompt below)"
---

# 12/06/2026 23:55 — Slice 1: routines sync + edit (+ D1 cursor fix)

## Done (commits c7402ea → aebd6db, pushed)

1. **D1 cursor fix** (`c7402ea`): `events_cursor` now stamps from *server*
   timestamps — backfill seeds from the newest workout `updated_at`;
   incremental sync advances to the newest event timestamp (`workout.
   updated_at` / `deleted_at`) and stays put when no events arrive. Local
   `utcnow` could skip events created between fetch and stamp; replaying the
   newest item is idempotent. 3 regression tests.
2. **Routines sync** (`9f88e29`, F1): client gains `GET /v1/routines`,
   `GET /v1/routines/{id}`, `GET /v1/routine_folders`, `PUT /v1/routines/{id}`.
   Cache gains `routines.json` / `archived_routines.json` /
   `routine_folders.json` (saved before meta — same rollback discipline).
   Sync refreshes them non-fatally; a vanished routine archives (never
   destroyed), a *failed/partial* fetch archives nothing (pinned by test).
3. **Vault notes** (`83284d7`, F1): `Hevy/Routines/<title>.md`, one managed
   note per routine. Frontmatter carries the FULL editable spec (template
   ids, sets, rep_range, rest_seconds, superset_id) so a note round-trips
   into the push parser; body renders readable set tables. Deleted routines'
   notes move to `Archive/`; a reused title can never drag the active note
   into the archive (guard + test).
4. **`push routine <file> [--dry-run]`** (`aebd6db`, F2): parses any note
   with `type: hevy-routine` frontmatter → `GET` live routine → prints a
   human-readable diff (title/notes/exercise adds/removes/set changes) →
   `PUT` full replacement. `--dry-run` stops after the preview; an identical
   note sends nothing. Round-trip guarantee pinned: an unedited vault note
   diffs clean. Routine sets reject `rpe` (API supports it on workout sets
   only). Explicit-push fence unchanged.

## Verify

`python -m pytest tests -q` → **85 passed** · `python -m ruff check
hevy_brain tests` → clean. All offline (fixtures/mocks; no real account).

## Decisions taken (SAMRATH.md §3 defaults, stated)

- **Edit flow = draft copy, not in-place**: the hourly `HevyBrain Sync`
  task regenerates managed notes, so an in-place frontmatter edit can be
  overwritten before it's pushed. Documented flow (also printed in every
  routine note): duplicate into `Routines/Drafts/`, edit, `push routine`.
  The parser accepts either file.
- **Diff preview always prints before the PUT**; no extra confirmation
  prompt (the explicit command is the consent), `--dry-run` for preview-only.
- Vanished routines keep their last payload in `archived_routines.json` —
  replace-set semantics only apply to a complete, successful fetch.

## Watch / gotchas

- **PUT /v1/routines/{id} not yet exercised live.** Shapes follow the
  OpenAPI spec parsed 12/06/2026 (incl. `GET /v1/routines/{id}` and the
  `{"routine": {...}}` wrapper, which `unwrap_routine()` tolerates in
  several shapes). First real run: `hevy-brain push routine <file>
  --dry-run` against a trivial edit, then a real push.
- Routines land in the vault automatically on the next hourly `full` run —
  no manual step. Check `Hevy/Routines/` after ~the next top of the hour.
- `routine_folders` keys are stringified ids (JSON round-trip safety).

## Carry-on prompt (next slice — E3 + E5, fresh session)

> Read CLAUDE.md + HANDOFF.md, then the roadmap note
> `C:\Users\samra\Atlas\projects\hevy-brain-roadmap.md` (§E3/E5) and the
> consumption rules in `C:\Users\samra\Atlas\_meta\routing.md`. Build
> **slice 2: knowledge bridge + provenance labels** — (a) `knowledge/`
> reader module implementing the routing.md consumption order (topic page →
> claim links → concept tags → claims-index → grep notes/), returning
> claims as text + evidence tag + `[[id#^claim-xx]]` link; read-only by
> construction, never touch `sources/` or write any pipeline folder
> (fences); (b) E5 provenance labels — every claim quoted in coach/briefing
> output marked `cited` (with link) or `general-knowledge`. Offline tests
> with fixture vault folders, ruff clean, one slice, commit per coherent
> step, push at end, update HANDOFF + dated handoff + carry-on prompt.
> Last commit: aebd6db. Locked: explicit-push fence; free tiers; repo
> private until key rotation. Do not re-litigate.
