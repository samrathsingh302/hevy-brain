---
status: done
agent: main-session (same session as 2120 entry, second slice)
goal: Brainstorm new features; store the output as Obsidian notes per Samrath's ask
outcome: >
  Feature roadmap written to the vault at projects/hevy-brain-roadmap.md —
  17 ideas across insights/write-back/coach/engineering, each grounded in
  the real cache (e.g. 62-day tracking lapse since 11/04/2026, Incline DB
  60→84 kg progression). Recommended next three: B1 next-session targets,
  A3+A4 lapse nudge + session patterns, A1 charts; D3 (register scheduled
  tasks) named as the freshness gate, D1 cursor fix folds into the first
  sync slice. projects/hevy-brain.md updated (stale key gotcha fixed, rename
  + private-status recorded, roadmap linked). HANDOFF gained a pointer only
  — the roadmap's single home is the vault note. CI from the 2120 push
  confirmed green on main (Lint + Test); dependabot auto-closed the
  superseded PRs and opened a green ruff 0.15.17 bump (unmerged).
gotchas: >
  Roadmap is a brainstorm, not committed scope — promote ideas via session
  slices and record decisions in HANDOFF. B3 routines sync must verify the
  /routines endpoint exists before any build. Body-measurement data stays
  vault-local (locked decision).
carry-on: >
  None needed — brainstorm complete. Next manual-drive slice unchanged:
  register scheduled tasks, then pick roadmap item B1.
---
