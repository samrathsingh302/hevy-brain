---
status: done
agent: main-session (CV-readiness overhaul)
goal: Bring the repo in line with PROJECT_KICKOFF/_shared-context and make it CV-worthy
outcome: >
  Renamed GitHub repo HA-hevy → hevy-brain (description+topics set, remote
  updated, old URL redirects). Recruiter-grade README: mermaid architecture,
  engineering-notes section, real screenshots rendered from actual generated
  notes via headless Edge (assets/, body data excluded — locked decision).
  config.toml untracked (config.example.toml added; safe default
  vault_staging/). Deps single-sourced in pyproject (requirements.txt
  removed, ruff pinned ==0.15.16, authors/license/urls added); actions bumped
  to checkout v6.0.3 + setup-python v6.2.0 superseding dependabot PRs
  #1/#2/#5. HA fork leftovers purged (issue templates, dependabot ignore).
  CHANGELOG.md created; HANDOFF.md demoted to pointer form per
  ORCHESTRATION §15. Background researcher agent ran a full-history
  pre-publication audit: CLEAN across 107 commits incl. unreachable blobs —
  one LOW finding (HANDOFF advertised live-key chat exposure) resolved by
  rewriting it into the neutral pre-public checklist. 60 tests + ruff
  verified green after every change.
gotchas: >
  Repo stays PRIVATE — flipping public is Samrath's explicit call, gated on
  the HANDOFF pre-public checklist (rotate HEVY_API_KEY first; audit verdict
  already recorded). Permission classifier blocked closing the three
  superseded dependabot PRs — close manually or let dependabot auto-close.
  Local folder is still HA-hevy; rename only between sessions (breaks cwd +
  Atlas CLAUDE.md map otherwise). Screenshot HTML sources live in
  %TEMP%\hevy_render if the renders ever need regenerating.
carry-on: >
  None needed for the overhaul — complete. Next manual-drive slice remains
  registering the scheduled tasks (scripts\register_task.ps1), then the
  optional coach briefing run; cursor-stamp design question still parked
  (sync.py:84-101).
---

Session notes: locked decisions recorded in HANDOFF.md §Locked decisions.
CV bullet points were delivered in-chat (not a repo artefact).
