---
status: done
agent: main-session (same session, third slice)
goal: Capture Samrath's north-star use case; verify feasibility; update vault roadmap
outcome: >
  North star recorded (vault roadmap, projects/hevy-brain-roadmap.md):
  training-edit guidance grounded in Hevy data + the transcript knowledge
  base, with full plan write-back. Two researcher agents verified the
  load-bearing facts: (1) official Hevy OpenAPI spec has PUT
  /v1/routines/{id} and PUT /v1/workouts/{id} — plan editing fully feasible;
  no DELETE endpoints; PUT is full-replacement so edits are GET→mutate→PUT;
  routine sets lack RPE; Pro-only API. (2) atlas-pipeline knowledge layer:
  5 notes (all Huberman), claims typed DO/AVOID/INFO with evidence tags +
  timestamped citations; binding consumption rules in _meta/routing.md
  (never read sources/, never write pipeline folders, declare gaps rather
  than infer); corpus has ZERO hypertrophy-programming/nutrition content —
  logged gap. Roadmap rewritten around three pillars (guidance engine,
  knowledge bridge, write-back v2) with build order: F1+F2 routines
  sync/edit → E3 knowledge bridge + E5 provenance labels → E1 guide return.
gotchas: >
  E2 (programme redesign) is corpus-starved until programming podcasts are
  ingested via atlas-pipeline (E4 — cross-repo, human-gated; NOT a
  hevy-brain task). Any guidance output must label claims cited vs
  general-knowledge (E5). hevy-brain stays read-only on pipeline folders
  and never reads sources/ — treat as fences when E3 is built.
carry-on: >
  Next manual-drive slice: register scheduled tasks, then F1+F2 (routines
  sync + edit round-trip with diff preview) per the roadmap build order.
---
